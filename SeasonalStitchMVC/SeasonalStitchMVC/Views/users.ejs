<!DOCTYPE html>
<html>
<head>
    <title>Users - Seasonal Stitch</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar">
        <a href="/" class="brand">Seasonal Stitch</a>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/inventory">Inventory</a>
            <a href="/admin/dashboard">Dashboard</a>
            <a href="/admin/orders">Orders</a>
            <a href="/admin/reports">Reports</a>
            <a href="/admin/users">Users</a>
            <a href="/logout">Logout</a>
        </div>
    </nav>

    <div class="container">
        <h2 class="page-title">Users</h2>
        <% if (!users || users.length === 0) { %>
            <p class="empty-state">No users found.</p>
        <% } else { %>
            <div class="orders-grid">
                <% users.forEach(entry => { %>
                    <div class="order-card">
                        <div class="order-card-header">
                            <div>
                                <p class="order-label">User</p>
                                <h3><%= entry.full_name %></h3>
                                <p class="order-muted"><%= entry.email %></p>
                            </div>
                            <div class="order-meta">
                                <span class="status-pill"><%= entry.role %></span>
                            </div>
                        </div>
                        <div class="order-card-footer">
                            <form action="/admin/users/<%= entry.user_id %>/points" method="post" class="status-form">
                                <label for="points-<%= entry.user_id %>" class="order-label">Points</label>
                                <div class="status-control">
                                    <input id="points-<%= entry.user_id %>" type="number" name="points" min="0" value="<%= entry.points || 0 %>">
                                    <button type="submit" class="btn btn-outline">Update</button>
                                </div>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</body>
</html>
